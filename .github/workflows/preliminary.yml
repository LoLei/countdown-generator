name: Preliminary

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  preliminary-checks:
    uses: LoLei/util/.github/workflows/yarn-preliminary.yml@main
    with:
      node-version: '14.18.2'
      format: true
      lint: true
